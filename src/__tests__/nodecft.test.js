
import { init } from '../nodecft'
describe('Test NodeCFT', () => {
    test('createStack should shoul execute without any issue', async () => {
        init();
        console.log(process.env)
    })
  })